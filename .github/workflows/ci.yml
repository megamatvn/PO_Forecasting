name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      SUPERPOWERS_DISABLE_TELEMETRY: "1"
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 11.9.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm test:coverage

      - name: Start isolated Supabase
        run: pnpm supabase start -x studio,imgproxy,edge-runtime,logflare,vector,mailpit

      - name: Configure app for local Supabase
        shell: bash
        run: |
          unset NEXT_PUBLIC_SUPABASE_URL NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY SUPABASE_SERVICE_ROLE_KEY SUPABASE_DB_URL SUPABASE_DB_POOLER_URL SUPABASE_DB_DIRECT_URL DATABASE_URL SUPABASE_PROJECT_REF E2E_DATABASE_MODE E2E_MODE E2E_RESET_TOKEN
          pnpm supabase status -o env > .supabase-env
          grep '^API_URL=' .supabase-env | sed 's/^API_URL=/NEXT_PUBLIC_SUPABASE_URL=/' > .env.local
          grep '^ANON_KEY=' .supabase-env | sed 's/^ANON_KEY=/NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=/' >> .env.local
          grep '^SERVICE_ROLE_KEY=' .supabase-env | sed 's/^SERVICE_ROLE_KEY=/SUPABASE_SERVICE_ROLE_KEY=/' >> .env.local
          grep '^DB_URL=' .supabase-env | sed 's/^DB_URL=/SUPABASE_DB_URL=/' >> .env.local

      - run: pnpm assert:isolated-e2e-env
      - run: pnpm test:db:local
      - run: pnpm build
      - run: pnpm check:secrets
      - run: pnpm verify:production-harness
      - run: pnpm e2e:local

      - name: Stop Supabase
        if: always()
        run: pnpm supabase stop --no-backup
